import React from 'react';
 
const ReactTblContext = React.createContext();
export const ReactTblContextProvider = ReactTblContext.Provider;
export const ReactTblContextConsumer = ReactTblContext.Consumer;
 
export default ReactTblContext;